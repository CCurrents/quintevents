<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>SFMCSubscriberId__c</fullName>
    <description>CaseSafeId for a single Subscriber Id, for Integration to Marketing Cloud (Reporting). Not for page layouts or user interface.</description>
    <externalId>false</externalId>
    <formula>IF(!ISBLANK(Contact__c), CASESAFEID(Contact__c),
IF(!ISBLANK(Lead__c), CASESAFEID(Lead__c),
&quot;&quot;))</formula>
    <inlineHelpText>CaseSafeId for a single Subscriber Id, for Integration to Marketing Cloud (Reporting). Not for page layouts or user interface.</inlineHelpText>
    <label>SFMC Subscriber Id</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
